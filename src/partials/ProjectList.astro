---
import projects from '@/data/projects';
import Image from '@/partials/Image.astro';

type Props = {
  projects: {
    alt: string,
    description: string,
    href: string
    title: string,
    src: string, 
  }[];
};

const { projects: Props } = Astro.props;
---

<section class="flex flex-col gap-2 pb-32 md:pb-48">
  <h2 class="fadeUpIn flex justify-between pb-4 text-lg">
    Recent Work<p class="mr-[-2px]">↓</p>
  </h2>
  
  {projects.map(
    ({ alt, title: pTitle, src, description: pDescription, href }, index: number) => (
      <a
        href={href}
        title={pTitle}
        class={`${ index === 0 ? 'fadeUpIn' : 'fadeUpInOnScroll'} flex flex-col gap-2`}
      >
        <div class="h-85 overflow-hidden rounded-xl">
          <Image
            src={src}
            alt={alt}
            class="transition-transform duration-700 hover:scale-110"
          />
        </div>
        <div class="mb-12 mt-2 flex text-lg md:pb-20">
          <h3 class="w-1/3">{pTitle}</h3>
          <p class="w-1/2">{pDescription}</p>
        </div>
      </a>
    )
  )}
</section>
